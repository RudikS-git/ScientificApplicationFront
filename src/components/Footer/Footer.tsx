import { FC } from "react";
import { StyledFooter } from "./Footer.styles";

const Footer: FC = () => {
    return (
        <StyledFooter>
            <footer>Vakilov Rudel, 2022</footer>
        </StyledFooter>
    );
};

export default Footer;
